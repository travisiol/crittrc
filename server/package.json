{
  "name": "crittr-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "CRITTR world server: rooms, movement, jobs, the trader, chat. Authoritative for everything that becomes gold.",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "test": "tsx --test test/*.test.ts",
    "typecheck": "tsc --noEmit",
    "snapshot": "tsx src/snapshot.ts"
  },
  "dependencies": {
    "viem": "^2.56.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/ws": "^8.5.13",
    "tsx": "^4.19.2",
    "typescript": "^5"
  }
}
